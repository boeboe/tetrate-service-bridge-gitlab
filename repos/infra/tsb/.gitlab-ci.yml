stages:
  - mp
  - cp
variables:
  TERM: xterm
mp:
  artifacts:
    untracked: true
    paths:
      - output/
  stage: mp
  script:
    - ./mp.sh install
cp:
  artifacts:
    untracked: true
    paths:
      - output/
  needs:
    - mp
  stage: cp
  script:
    - ./cp.sh install

