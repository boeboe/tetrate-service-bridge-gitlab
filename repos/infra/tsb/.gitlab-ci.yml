stages:
  - mp
  - cp
variables:
  TERM: xterm
mp:
  artifacts:
    paths:
      - output/*
  stage: mp
  script:
    - ./mp.sh install
cp:
  artifacts:
    paths:
      - output/*
  needs:
    - mp
  stage: cp
  script:
    - ./cp.sh install

