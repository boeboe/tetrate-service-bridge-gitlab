---
stages:
  - prereq
  - ldap
  - ui
variables:
  TERM: xterm
prereq:
  allow_failure: false
  script: 
    - ./prereq.sh check
  stage: prereq
ldap:
  needs:
    - prereq
  script:
    - ./ldap.sh sync
  stage: ldap
ui:
  needs:
    - ldap
  script:
    - ./ldap.sh ui
  stage: ui
