---
apiVersion: v1
kind: Service
metadata:
  name: svc-a
  namespace: ns-a
  labels:
    app: svc-a
    service: svc-a
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: svc-a
