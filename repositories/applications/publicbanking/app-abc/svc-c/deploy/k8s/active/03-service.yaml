---
apiVersion: v1
kind: Service
metadata:
  name: svc-c
  namespace: ns-c
  labels:
    app: svc-c
    service: svc-c
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: svc-c
