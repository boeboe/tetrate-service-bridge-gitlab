---
apiVersion: v1
kind: Service
metadata:
  name: app-w
  namespace: ns-w
  labels:
    app: app-w
    service: app-w
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: app-w
