---
apiVersion: v1
kind: Service
metadata:
  name: back-cash2
  namespace: back-cash2
  labels:
    app: back-cash2
    service: back-cash2
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: back-cash2
