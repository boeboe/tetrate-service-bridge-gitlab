---
apiVersion: v1
kind: Service
metadata:
  name: back-cash1
  namespace: back-cash1
  labels:
    app: back-cash1
    service: back-cash1
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: back-cash1
