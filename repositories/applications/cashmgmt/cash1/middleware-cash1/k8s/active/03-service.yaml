---
apiVersion: v1
kind: Service
metadata:
  name: mid-cash1
  namespace: mid-cash1
  labels:
    app: mid-cash1
    service: mid-cash1
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: mid-cash1
