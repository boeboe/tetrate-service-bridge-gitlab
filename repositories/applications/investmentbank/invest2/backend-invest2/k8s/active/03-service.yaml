---
apiVersion: v1
kind: Service
metadata:
  name: back-invest2
  namespace: back-invest2
  labels:
    app: back-invest2
    service: back-invest2
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: back-invest2
