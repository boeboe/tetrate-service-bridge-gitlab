---
apiVersion: v1
kind: Service
metadata:
  name: back-invest1
  namespace: back-invest1
  labels:
    app: back-invest1
    service: back-invest1
spec:
  ports:
    - port: 80
      targetPort: 8000
      name: http
    - port: 3000
      targetPort: 3000
      name: tcp
  selector:
    app: back-invest1
